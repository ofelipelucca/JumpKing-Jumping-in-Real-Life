#pragma once

namespace IOValues {

	/// @brief Index of the selected square. -1 when when no squares are selected.
	/// @details Indice do quadrado selecionado. -1 quando nenhum quadrado esta selecionado.
	extern int selectedSquare;

	/// @brief Boolean value to verify the click position.
	/// @details Valor booleano para verificar a posicao do clique.
	extern bool clickOnSquare;
}
